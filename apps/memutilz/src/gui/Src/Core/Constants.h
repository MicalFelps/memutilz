#pragma once

namespace Core {
    namespace Constants {
        // Context
        const char C_GLOBAL[] = "Context.Global";
        // Actions
        const char CONTINUE[] = "Debug.Continue";
    }
}